import { describe, it, expect } from "vitest";
import MeasurementsForm from "./MeasurementsForm.jsx";

describe("Measurements component", () => {
  it("is defined", () => {
    expect(MeasurementsForm).toBeDefined();
  });
});
