import { describe, it, expect } from "vitest";
import MeasurementsDisplay from "./MeasurementsDisplay.jsx";

describe("Measurements component", () => {
  it("is defined", () => {
    expect(MeasurementsDisplay).toBeDefined();
  });
});
